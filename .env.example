# MySQL — Hostinger panelindən KOPYALAYIN (Databases → MySQL)
# Adlar adətən belə olur: u884620139_elite_emlak / u884620139_user_emlak
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=u884620139_elite_emlak
DB_USER=u884620139_user_emlak
DB_PASSWORD=buraya_duzgun_sifre

# Server (Hostinger özü PORT verə bilər)
PORT=3000

# JWT — production-da mütləq dəyişin
JWT_SECRET=change-me-to-a-long-random-string

# Admin (schema seed): username=admin  password=admin123
#
# Hostinger Node.js:
# 1) .env faylını app root-a qoyun VƏ YA
# 2) hPanel → Websites → eliteemlak.az → Node.js → Environment variables
# 3) User-i bazaya bind edin (Add user to database → ALL PRIVILEGES)
# 4) schema.sql import (phpMyAdmin)
# 5) App Restart
